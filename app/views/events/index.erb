<h3>Your Events</h3><br>
<table width="600">
  <tr>
    <th><center>Friends</center></th>
    <th><center>Family</center></th>
  </tr>
  <tr>
    <td>
      <%if @friends.empty?%>
        <a href="events/new">Schedule some time with friends</a>
      <%else%>
        <% @friends.each do |event| %>
          <li> <a href="/events/<%= event.id %>"> <%= event.name %> </a> | <%=event.date%></li>
        <% end %>
      <% end %>
    </td>
    <td>
      <%if @family.empty?%>
        <a href="events/new">Schedule some time with family</a>
      <%else%>
        <% @family.each do |event| %>
          <li> <a href="/events/<%= event.id %>"> <%= event.name %> </a> | <%=event.date%></li>
        <% end %>
      <% end %>
    </td>
  </tr>
</table>
<table width="600">
  <tr>
    <th><center>Work</center></th>
    <th><center>Health</center></th>
  </tr>
  <tr>
    <td>
      <%if @work.empty?%>
        <a href="events/new">Schedule some time for business</a>
      <%else%>
        <% @work.each do |event| %>
          <li> <a href="/events/<%= event.id %>"> <%= event.name %> </a> | <%=event.date%></li>
        <% end %>
      <% end %>
    </td>
    <td>
      <%if @health.empty?%>
        <a href="events/new">Schedule a some time to take care of yourself</a>
      <%else%>
        <% @health.each do |event| %>
          <li> <a href="/events/<%= event.id %>"> <%= event.name %> </a> | <%=event.date%></li>
        <% end %>
      <% end %>
    </td>
  </tr>
</table>
